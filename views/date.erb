<!DOCTYPE html> 

 <body>
  <h1><%= "Today" %></h1>
  <% time = Time.now %> 
   <p><%= "The date is " + time.strftime("%A") + ", " + time.strftime("%B") + " " + time.strftime("%d") + ", " + time.strftime("%Y") + "." %></p>
 </body>
</html>