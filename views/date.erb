<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <h1>Today</h1>
    <% date = DateTime.now %>
    <p><%= date.strftime("The date is %A, %B %e, %Y") %></p>
  </body>
</html>
