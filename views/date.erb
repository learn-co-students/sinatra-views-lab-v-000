<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <% d = DateTime.now %>
    <% date = "#{d.strftime("%A, %B %d, %Y")}" %>
    <h1>The date is <%=date%></h1>
  </body>
</html>
