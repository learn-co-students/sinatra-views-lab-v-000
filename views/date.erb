<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <h1>Today</h1>
    <% date = DateTime.now%>
    <p><%= "The date is #{date.strftime('%A')}, #{date.strftime('%B')} #{date.strftime('%d')}, #{date.strftime('%G')}." %></p>
  </body>
</html>
