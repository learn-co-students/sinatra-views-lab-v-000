<!-- Create a template called date.erb in views that gets rendered via 
GET /date. It should contain an h1 with the content Today 
Using ERB tags, and the DateTime library, display today's date in a
 p tag. The date should be formatted to look something like this 
 The date is Wednesday, November 18, 2015. -->

<!DOCTYPE html>
<html>
    <head>
        <title>My First View!!</title>
    </head>
    <body>
        <h1><%= Date.today %></h1>
        <% date = Time.now %>
        <p><%= "The date is #{Date.today.strftime("%A")}, #{date.strftime("%B %d, %Y")}" %></p>
    </body>
</html>