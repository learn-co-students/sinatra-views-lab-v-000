<!-- It should contain an `h1` with the content `Today`
Using ERB tags, and the DateTime library, display today's date in a `p` tag. The date should be
formatted to look something like this `The date is Wednesday, November 18, 2015`. -->

<html>
  <head>date
    <% require 'date' %>
  </head>
  <body>
    <h1>Today</h1>
    <% d = DateTime.now %>
    <p> <%= d.strftime("The date is %A, %B %e, %Y")%> </p>
  </body>
</html>
