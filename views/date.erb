<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% time = Time.new %>
    <% year = time.year %>
    <% day = time.day %>
    <% wday = time.wday %>
    <% month = time.month %>
    <% formatted = time.strftime("%A, %B %d, %Y") %>
    <h1>Today</h1>
    <p>The date is <%= formatted %></p>

  </body>
</html>
