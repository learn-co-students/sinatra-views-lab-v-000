<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <% t = Time.now %>
        <h1>Today</h1>
        <p>The date is <%= t.strftime("%A, %B %d, %Y") %> </p>

    </body>
</html>