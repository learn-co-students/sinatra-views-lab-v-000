<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <% today = Time.now.strftime('%A, %B %d, %Y') %>
        <h1><%= "The date is " + today  %></h1>
    </body>
</html>