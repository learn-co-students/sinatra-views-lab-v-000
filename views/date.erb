<!DOCTYPE html>
<html>
  <body>
    <%= today= Time.now %>
    <h1>Today</h1>
    <p><%= "The date is #{today.strftime("%A")}, #{today.strftime("%B")} #{today.strftime("%d")}, #{today.strftime("%Y")}"%>
  </body>
</html>
