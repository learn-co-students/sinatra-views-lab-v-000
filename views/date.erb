<!DOCTYPE html>
<html>
    <head>
        <title>Place holder!</title>
    </head>
    <body>
        <h1>Today</h1>
        <% t=Time.now %>
        <p> <%="The date is #{t.strftime("%A, %B %d, %Y")}" %></p>
    </body>
</html>